int majorityElement(int* nums, int n){
  int i, j, curr, count;
  for(i=0; i<n; i++){
    curr = nums[i];
    count = 0;
    for(j=0;j<n;j++){
      if(curr == num[j] ++count;
      if(count >= (n/2)+1) return curr;
   }
   return -1;
}   
